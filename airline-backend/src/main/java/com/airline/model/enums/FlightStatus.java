package com.airline.model.enums;

public enum FlightStatus {
    SCHEDULED,
    ON_TIME,
    DELAYED,
    BOARDING,
    DEPARTED,
    IN_FLIGHT,
    LANDED,
    ARRIVED,
    CANCELLED,
    DIVERTED
}
